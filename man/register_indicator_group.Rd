% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-indicator-groups.R
\name{register_indicator_group}
\alias{register_indicator_group}
\title{Register or override an indicator group (profile)}
\usage{
register_indicator_group(
  name,
  value,
  on_conflict = c("replace", "merge", "error")
)
}
\arguments{
\item{name}{A non-empty string: the group/profile name to create or modify.}

\item{value}{A \strong{named list}: each element is a category containing a character
vector of indicator codes.}

\item{on_conflict}{One of \code{"replace"}, \code{"merge"}, or \code{"error"}. \code{"merge"} performs
a union per category (\code{sort(unique(c(old, new)))}).}
}
\value{
Invisibly returns \code{name}.
}
\description{
Adds or modifies a group definition by \strong{name}. If the name already exists,
use \code{on_conflict = "replace"} to replace the entire definition, or
\code{on_conflict = "merge"} to \strong{union} indicators \emph{per category}.
}
\details{
Reserved names \code{"auto"} and \code{"custom"} cannot be registered.
\itemize{
\item If \code{name} matches a built-in (e.g. \code{"rmncah"}), the override will be layered
on top of the built-in according to \code{on_conflict}.
\item The merged, live view is available via \code{.get_all_groups()} or
\code{\link[=get_indicator_groups]{get_indicator_groups()}}.
}
}
\examples{
# Replace RMNCAH completely
register_indicator_group(
  "rmncah",
  list(anc = c("anc1","anc4"), idelv = c("sba")),
  on_conflict = "replace"
)

# Extend RMNCAH by merging into existing categories (union)
register_indicator_group(
  "rmncah",
  list(hiv = c("hiv_test","pmtct1")),
  on_conflict = "merge"
)

}
